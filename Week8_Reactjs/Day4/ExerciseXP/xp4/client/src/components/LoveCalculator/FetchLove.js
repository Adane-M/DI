export const FetchLove = (props) => {
    return (
        //onclick fetches the url to recive the results
        <>
        
            <button onClick={props.handleClick}>Result</button>
        </>
    )

}
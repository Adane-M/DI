export const Lname =(props)=>{

    return (
        //the seconde name we need for the fetch link
       <p>Seconde name : <input type='text' name='sname' onChange={props.handleChange}/> </p>
    )
}